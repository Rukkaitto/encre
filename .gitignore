build/
# The anti-alias A/B's second build tree: same sources, ENCRE_AA_THRESHOLDS_4BIT on.
build-aa/
# The AddressSanitizer tree. Same reason as build-aa/, and it was TRACKED --
# 412 files of objects, .dSYM bundles and CMake caches carrying absolute paths.
build-asan/
.pio/
.cache/
__pycache__/
*.pyc
.DS_Store
platformio.local.ini

# Visual brainstorming companion scratch (superpowers)
.superpowers/

# Device captures. CLAUDE.md uses run.log by convention; these are
# transient evidence, and what they established belongs in the spec.
*.log

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
